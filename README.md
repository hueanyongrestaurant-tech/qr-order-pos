
  # Mobile Food Ordering App

  This is a code bundle for Mobile Food Ordering App. The original project is available at https://www.figma.com/design/AjXuGZM4aUrCbNvkHt80sG/Mobile-Food-Ordering-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
# qr-order-pos

